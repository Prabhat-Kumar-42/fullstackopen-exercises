const home = "/";
const anecdoteForm = "/create";
const about = "/about";
const anecdotes = "/anecdotes/";
const specificAnecdote = "/anecdotes/:id";

const anecdoteUrls = {
  home,
  about,
  anecdotes,
  anecdoteForm,
  specificAnecdote,
};

export default anecdoteUrls;
